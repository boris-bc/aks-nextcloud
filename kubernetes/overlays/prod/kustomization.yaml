apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nextcloud

resources:
  - ../../base

commonLabels:
  environment: prod

patchesStrategicMerge:
  - deployment-patch.yaml

replicas:
  - name: nextcloud
    count: 3
